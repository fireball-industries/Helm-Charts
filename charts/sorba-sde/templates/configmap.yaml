apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "sorba-sde.fullname" . }}
  labels:
    {{- include "sorba-sde.labels" . | nindent 4 }}
data:
  # Add any custom configuration here
  # This can be extended based on SORBA SDE configuration needs
  init.sh: |
    #!/bin/sh
    echo "SORBA SDE Initializing..."
    {{- if .Values.network.ipForward }}
    echo "Enabling IP forwarding..."
    {{- end }}
